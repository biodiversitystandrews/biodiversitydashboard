name: Update Parquet Data from Google Drive

on:
  repository_dispatch:
    types: [new-gpkg-file]
  workflow_dispatch:
    inputs:
      file_id:
        description: Google Drive file ID of the observation GPKG
        required: true
        type: string

# Every generated-data workflow uses this group so two runs cannot push
# different snapshots of main at the same time.
concurrency:
  group: dashboard-generated-data-writes
  cancel-in-progress: false

jobs:
  build-and-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install GDAL and other dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gdal-bin libgdal-dev

      - name: Install Python packages
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt -r requirements-workflows.txt

      - name: Authenticate to Google Drive
        run: echo '${{ secrets.GDRIVE_CREDENTIALS_DATA }}' > gdrive-credentials.json

      - name: Download .gpkg file from Google Drive (service account)
        run: python download_from_gdrive.py "${{ github.event.client_payload.file_id || inputs.file_id }}" "data.gpkg"

      - name: Run data conversion script
        run: python gpkgtoparquet.py data.gpkg

      - name: Commit and push if there are changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # Generated data and the shared species cache must be committed together.
          git add data/data.parquet species_api_cache.csv
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Chore: Update Parquet data and API cache"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push origin "HEAD:${GITHUB_REF_NAME}"
          fi
